/// HelmChartConfig represents additional configuration for the installation of Helm chart release. This
/// resource is intended for use when additional configuration needs to be passed to a HelmChart that is
/// managed by an external system.
///
/// This module was generated from the CustomResourceDefinition at
/// <file:///Users/lucsoft/Developer/infra-hmsys-sys/packages/io.k3s.HelmController/>.
module io.cattle.helm.v1.HelmChartConfig

extends "@k8s/K8sResource.pkl"

import "@k8s/apimachinery/pkg/apis/meta/v1/ObjectMeta.pkl"

fixed apiVersion: "helm.cattle.io/v1"

fixed kind: "HelmChartConfig"

/// Standard object's metadata.
///
/// More info: <https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata>.
metadata: ObjectMeta?

/// HelmChartConfigSpec represents additional user-configurable details of an installed and configured
/// Helm chart release. These fields are merged with or override the corresponding fields on the related
/// HelmChart resource.
spec: Spec?

/// HelmChartConfigSpec represents additional user-configurable details of an installed and configured
/// Helm chart release. These fields are merged with or override the corresponding fields on the related
/// HelmChart resource.
class Spec {
  /// Configures handling of failed chart installation or upgrades. - `abort` will take no action and
  /// leave the chart in a failed state so that the administrator can manually resolve the error. -
  /// `reinstall` will perform a clean uninstall and reinstall of the chart; this is the default
  /// behavior. - `retry` will attempt to retry the install or upgrade whenever chart configuration
  /// changes.
  failurePolicy: ("abort" | "reinstall" | "retry")?

  /// Set to true if helm should configure server-side apply to force changes when conflicts arise in
  /// ownership of managed fields. Helm CLI positional argument/flag: `--force-conflicts`
  forceConflicts: Boolean?

  /// Set to true if helm should enable server-side apply when updating objects. Defaults to `true` for
  /// install, and `auto` for upgrade. - `true` enables server-side apply. - `false` disables server-side
  /// apply. - `auto` enables server-side apply if the chart was installed with server-side apply
  /// enabled. Helm CLI positional argument/flag: `--server-side`
  serverSide: ("true" | "false" | "auto")?

  /// Override complex Chart values via structured YAML. Takes precedence over options set via
  /// valuesContent. Helm CLI positional argument/flag: `--values`
  values: Null?

  /// Override complex Chart values via inline YAML content. Helm CLI positional argument/flag:
  /// `--values`
  valuesContent: String?

  /// Override complex Chart values via references to external Secrets. Helm CLI positional
  /// argument/flag: `--values`
  valuesSecrets: Listing<ValuesSecret>?
}

/// SecretSpec describes a key in a secret to load chart values from.
class ValuesSecret {
  /// Ignore changes to the secret, and mark the secret as optional. By default, the secret must exist,
  /// and changes to the secret will trigger an upgrade of the chart to apply the updated values. If
  /// `ignoreUpdates` is true, the secret is optional, and changes to the secret will not trigger an
  /// upgrade of the chart.
  ignoreUpdates: Boolean?

  /// Keys to read values content from. If no keys are specified, the secret is not used.
  keys: Listing<String>?

  /// Name of the secret. Must be in the same namespace as the HelmChart resource.
  name: String?
}
