import "pkl:yaml"

import "@deepToTyped/deepToTyped.pkl"

import "./SopsSecret.pkl"

function includeSopsSecret(source: Resource | String): SopsSecret =
  deepToTyped.apply(
    SopsSecret.getClass(),
    new yaml.Parser {}.parse(source),
  ) as SopsSecret
